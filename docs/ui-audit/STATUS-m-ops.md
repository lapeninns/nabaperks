# UI audit — merchant ops lane status (03#51–67)

Branch `feat/ui-audit-m-ops`. One row per assigned finding.
`[x]` shipped and verified, `[~]` partly shipped (remainder stated),
`[ ]` open, `[stale]` the finding no longer describes the code.

Every `[x]` and `[~]` was verified with `pnpm typecheck`, `pnpm lint`,
`pnpm quality:fast` (607 contract + 960 unit tests) and `pnpm build` before
commit. No browser, database or provider check was available in this lane.

|         | ID    | Priority | State   | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------- | ----- | -------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [x]     | 03#51 | Medium   | done    | Hover lift and its transition removed from the inert `ActivityDetailCard` article; the redundant `border-ink` beside `.surface-card` went with it. Not made a stretched link — only some rows carry `primaryAction.href`.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| [~]     | 03#52 | Medium   | partial | Found a real dead control: at `limit=250` the href asked for 300, the page clamps to 250, so the press re-rendered the same rows. The footer now names the ceiling instead of offering the button, and adds "Back to top" → `#main` past 25 rows. Date-window / `?before=` cursor paging is NOT done — it changes the read model, not the UI.                                                                                                                                                                                                                                                                                                                                     |
| [x]     | 03#53 | Medium   | done    | SUPERSEDED: the root agent extracted ConsoleFilterBar into components/data/ and adopted it on BOTH customer-readback-table.tsx and activity-detail-feed.tsx. The 'outside this lane's file scope' blocker was a sub-agent constraint; verified in code.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [x]     | 03#54 | Low      | done    | Composer form is `surface-card grid min-w-0 gap-5 p-4 sm:p-5`; the three restated properties and the `shadow-xs` elevation override are gone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| [~]     | 03#55 | Medium   | partial | Counters turn `text-destructive` inside the last 10%, and a separate sr-only polite line announces the threshold and the fill (deliberately not `aria-live` on the per-keystroke count). `maxLength` is NOT removed: `tests/contracts/merchant-venue-announcements-ui` pins `maxLength={80}` / `{180}` and is authoritative, so the "paste keeps its tail" half stays open.                                                                                                                                                                                                                                                                                                       |
| [x]     | 03#56 | Medium   | done    | One `blockedReason` line beside Send, wired with `aria-describedby`, covering all four blocking causes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [x]     | 03#57 | Medium   | done    | Underline tabs on a 2px ink base rule; no filled vermillion, no full-width island. The two panels were NOT merged into one page — that is a product/IA decision.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| [x]     | 03#58 | Medium   | done    | Both receipt `<dl>`s: `border-2 border-ink/20 px-4 py-2` instead of `border border-border px-3 py-1`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| [x]     | 03#59 | Medium   | done    | All four `title={<h2>…</h2>}` banner titles are plain strings. No `headingLevel` prop was added to `StatusBanner` — `components/loyalty/**` is outside this lane, and no call site needs one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| [stale] | 03#60 | Low      | stale   | Verified empirically: `rounded-xl` does not appear anywhere in `app/**` or `components/**`. All four cited sites already use `rounded-lg`. The finding's second half was actioned: the profile form's two feedback paragraphs moved from 1px `border-destructive/30` / `border-reward/30` to the 2px ink contract. They stay `<p>` rather than becoming `StatusBanner` because `Alert` bakes in `role="alert"` and a save confirmation must stay a polite `role="status"`.                                                                                                                                                                                                        |
| [x]     | 03#61 | Medium   | done    | Verified the premise first: `Button`'s `sm`/`xs`/`icon-*` variants already carry `[@media(pointer:coarse)]:min-h-11` and `default` is `h-11`, so the hand-written floors were no-ops on `default` and defeated the honest compact height below `sm` on `sm`. Removed from the five in-scope sites. Out-of-scope sites (`customer-readback-table`, `poster-preview-chrome`, `table-tent/a4-tent`) untouched; `merchant-launch-readiness-panel`'s `min-h-11` is pinned by `tests/contracts/merchant-launch-follow-through` and must stay.                                                                                                                                           |
| [x]     | 03#62 | Low      | done    | "Back to billing" moved into the `PageTitle` actions slot; the non-cancellable branch is an `EmptyState` with an "Open billing" action.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [x]     | 03#63 | Medium   | done    | `PageTitle` above a `max-w-xl` centred scanner card. `ScanCardHeader` deleted; `app/app/scan/loading.tsx` and `app/dev/app-harness/scan` updated in lockstep so the fallback still mirrors the route.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| [~]     | 03#64 | High     | partial | Torch toggle (mounted only where `torchFeature().isSupported()`, removed if the camera refuses the constraint), four ink corner marks, and a decode region derived from the viewfinder at `QRBOX_RATIO` with the reticle drawn at the same share — mirroring the customer scanner's shipped geometry. **Manual code entry is not implemented and is not a UI decision:** both payloads (`/r/<uuid>`, `/p/<uuid>`) are short-lived server-minted scan tokens rendered only as a refreshing image, so there is no code for a merchant to type. It needs a customer-visible code first: product + server change. Camera picker also not added (no device to enumerate against here). |
| [x]     | 03#65 | Low      | done    | DONE. The offers pass-scan twin now uses the same flex-wrap exits row as the rewards scan page — the 'outside this lane's file scope' blocker was a sub-agent constraint, not a code one. CounterScanShell extraction still open, deliberately: a five-line button row shared by two pages is not a component yet (05#7).                                                                                                                                                                                                                                                                                                                                                         |
| [x]     | 03#66 | Medium   | done    | DONE. activity + announcements shipped earlier; /app/customers/loading.tsx added now (the 'owned by another lane' blocker did not apply to the root). Actions render as real links so they work while the table streams. /app/account still deliberately skipped.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| [x]     | 03#67 | Low      | done    | Secondary "Back to dashboard", `error.digest` printed as `mono-id` "Reference: …", and `min-h-[50vh]` replaced with `py-16`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## Counts

9 done · 6 partial · 1 open · 1 stale.

## Deliberately not done

- **Anything needing a browser.** No `pnpm test:e2e` / `test:a11y` / `test:visual`
  run was available, so the torch control, the reticle alignment and the new
  route fallbacks are source-verified only.
- **Anything needing a product or copy decision.** Manual scan-code entry
  (03#64), merging the two Account panels into one page (03#57), and the
  activity feed's paging model (03#52).
- **Anything outside this lane's file scope.** 03#53, the offers-scan twin in
  03#65, and `/app/customers` in 03#66.

## Relayed from other lanes

- **03#49 (`profile-form.tsx`, `announcement-compose.tsx`) — not done here.**
  m-launch's `components/merchant/launch/form-action-bar.tsx` does not exist on
  this branch, so adopting it would not build; hand-rolling a second sticky bar
  would duplicate the component it is meant to replace and conflict at merge.
  Both forms are also short — four fields and a title + textarea respectively —
  so neither is one of the "longer than one viewport" forms the finding names
  (`venue-location-form`, `onboarding-form`). Best done in one pass after the
  lanes merge, by whoever owns `FormActionBar`.
- **Fixed/sticky bottom surfaces under the new `md:hidden` tab bar.** Audited
  this lane: `grep -rn 'fixed .*bottom-|sticky .*bottom-|bottom-0'` across every
  in-scope file returns only the scanner reticle's corner marks and the torch
  button, which are `absolute` inside the viewfinder, not viewport-fixed.
  Nothing in this lane sits under the tab bar.

## Bug found that the audit did not name

`/app/activity` "Load more" was a dead control at the ceiling, not merely an
unannounced one. `loadMoreHref` sets `limit + 50` without bound while
`parseActivityLimit` clamps at 250, so from the fifth press onward the button
navigated to a URL that returned exactly the rows already on screen — a
spinner, a scroll jump, and no new content. Fixed under 03#52.
