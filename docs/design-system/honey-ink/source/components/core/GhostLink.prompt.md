Underlined tertiary text action — the escape hatch under a primary InkButton.

```jsx
<GhostLink onClick={skip}>Maybe later</GhostLink>
```

Use for deferrals ("Skip for now", "Back to my card"). Never style it like a button; 44px min tap height is built in.
