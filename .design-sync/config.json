{
  "pkg": "nabaperks",
  "globalName": "WetInk",
  "projectId": "7d268003-c754-405b-9942-d12b07e66e44",
  "shape": "package",
  "srcDir": "components",
  "tsconfig": ".design-sync/tsconfig.ds.json",
  "entry": ".design-sync/ds-entry.ts",
  "cssEntry": ".ds-sync/wetink.css",
  "extraFonts": [".design-sync/fonts/wetink-fonts.css"],
  "guidelinesGlob": ["DESIGN.md"],
  "dtsPropsFor": {
    "Alert": "  /** Visual tone. \"destructive\" tints title/description + icon with the destructive color. @default \"default\" */\n  variant?: \"default\" | \"destructive\"\n  // + native <div> props (role defaults to \"alert\")",
    "Badge": "  /** Visual tone. `reward` is the leaf-green loyalty accent. @default \"default\" */\n  variant?: \"default\" | \"secondary\" | \"reward\" | \"destructive\" | \"outline\" | \"ghost\" | \"link\"\n  /** Render as Radix Slot, merging props onto the single child instead of a <span>. @default false */\n  asChild?: boolean\n  // + native <span> props",
    "Card": "  /** Density. \"sm\" tightens --card-spacing from 5 to 4. @default \"default\" */\n  size?: \"default\" | \"sm\"\n  // + native <div> props",
    "Input": "  // native <input> props only (type, placeholder, value, disabled, aria-invalid, …)\n  // Styling hooks: disabled => dimmed + non-interactive; aria-invalid => destructive border/ring.",
    "Label": "  // props of Radix LabelPrimitive.Root (≈ native <label>, incl. htmlFor)\n  // Auto-dims when an associated peer is :disabled or a wrapping group is data-[disabled=true].",
    "Separator": "  /** @default \"horizontal\" */\n  orientation?: \"horizontal\" | \"vertical\"\n  /** If true, removes semantic role (pure visual divider). @default true */\n  decorative?: boolean\n  // + Radix Separator.Root props",
    "Progress": "  /** 0–100 fill percentage. Indicator translates by -(100 - value)%. Falsy => 0. */\n  value?: number\n  // + Radix Progress.Root props (e.g. max, getValueLabel)",
    "Empty": "  /** Visual treatment for the media slot. `icon` renders a rounded muted tile sized for a single glyph. @default \"default\" */\n  variant?: \"default\" | \"icon\"\n  // + native React.ComponentProps<\"div\">\n  ",
    "Field": "  /** Row layout. `responsive` switches vertical→horizontal at @md within a FieldGroup. @default \"vertical\" */\n  orientation?: \"vertical\" | \"horizontal\" | \"responsive\"\n  // + native React.ComponentProps<\"div\">\n  ",
    "Sheet": "  /** Edge the sheet slides from. @default \"right\" */\n  side?: \"top\" | \"right\" | \"bottom\" | \"left\"\n  /** Show the corner close (X) button. @default true */\n  showCloseButton?: boolean\n  // + Radix Dialog.Content props\n  ",
    "Sidebar": "  /** @default \"left\" */ side?: \"left\" | \"right\"\n  /** \"icon\" collapses to a slim icon rail; \"none\" = static full-height column (no offcanvas/mobile Sheet). @default \"offcanvas\" */\n  collapsible?: \"offcanvas\" | \"icon\" | \"none\"\n  // + native div props\n  ",
    "Toaster": "/** Corner the stack anchors to. @default \"bottom-right\" */\nposition?: \"top-left\" | \"top-center\" | \"top-right\" | \"bottom-left\" | \"bottom-center\" | \"bottom-right\"\n/** Use semantic success/error/etc. colors. */ richColors?: boolean\n/** ms before auto-dismiss. */ duration?: number\n/** Max simultaneous toasts. */ visibleToasts?: number\nexpand?: boolean\ncloseButton?: boolean\ntheme?: \"light\" | \"dark\" | \"system\"\n// + remaining sonner ToasterProps",
    "Logo": "/** Link target for the wordmark. */\n  href?: string;\n  /** Accessible/visible brand name. */\n  label?: string;\n  /** Mark-only (wordmark goes `sr-only`) — for tight mobile headers. */\n  compact?: boolean;\n  /** Classes on the visible wordmark text; e.g. `hidden sm:inline` to drop it responsively. Ignored when `compact`. */\n  wordmarkClassName?: string;\n  className?: string;",
    "MonoTag": "children: React.ReactNode;\n  /** Spot-ink fill. `plain` is the bare outline pill. */\n  tone?: \"plain\" | \"accent\" | \"cobalt\" | \"ink\" | \"leaf\" | \"sun\";\n  /** Optional leading glyph from `@hugeicons/core-free-icons`. */\n  icon?: IconGlyph;\n  className?: string;",
    "ReceiptCard": "children: React.ReactNode;\n  /** Render the perforated zigzag receipt edge below the card. */\n  edge?: boolean;\n  /** Inner padding preset. */\n  padding?: \"none\" | \"sm\" | \"md\" | \"lg\";\n  /** Classes on the outer wrapper that carries the rotation/edge. */\n  wrapperClassName?: string;\n  /** Stamp-family tilt — receipts sit a touch off-square. */\n  rotated?: boolean;\n  /** Fire the paper shake (WetInkShake) on mount — e.g. when a fresh stamp lands. */\n  shaken?: boolean;\n  className?: string;",
    "VenueMark": "/** Two-letter roundel text. Provide this OR `name`. */\n  initials?: string;\n  /** Business name to derive initials from, e.g. \"Bridge Street Coffee\" → BS. */\n  name?: string;\n  /** Optional uppercase caption under the roundel. */\n  caption?: string;\n  /** Diameter in px (font scales from it). */\n  size?: number;\n  className?: string;",
    "Icon": "/** A Hugeicons glyph reference, e.g. `Coffee01Icon` from `@hugeicons/core-free-icons`. */\n  icon: IconGlyph;\n  /** Size in px (or use a `size-*` class). */\n  size?: number;\n  strokeWidth?: number;\n  /** Accessible name. Omit for decorative icons (rendered `aria-hidden`). */\n  label?: string;\n  className?: string;",
    "Eyebrow": "children: React.ReactNode;\n  className?: string;",
    "SectionHeader": "/** Small uppercase kicker above the title. */\n  eyebrow?: React.ReactNode;\n  /** Section heading (renders as <h2>). */\n  title: React.ReactNode;\n  description?: React.ReactNode;\n  /** Right-aligned action slot, e.g. <Button>s. */\n  actions?: React.ReactNode;\n  className?: string;",
    "MetricTile": "/** Uppercase stat label. */\n  label: React.ReactNode;\n  /** The figure (rendered tabular). */\n  value: React.ReactNode;\n  /** Optional supporting line under the value. */\n  helper?: React.ReactNode;\n  /** Week-over-week trend chip; colour follows `direction`. Pass `null` to omit. */\n  trend?: { label: string; direction: \"up\" | \"down\" | \"flat\" } | null;\n  /** Optional leading glyph from `@hugeicons/core-free-icons`. */\n  icon?: IconGlyph;\n  className?: string;",
    "EmptyState": "title: React.ReactNode;\n  description?: React.ReactNode;\n  /** Action slot under the copy, e.g. a primary <Button>. */\n  actions?: React.ReactNode;\n  /** Optional leading glyph from `@hugeicons/core-free-icons`, shown above the title. */\n  icon?: IconGlyph;\n  /** Heading level for the title (a11y). */\n  headingLevel?: 1 | 2 | 3;\n  className?: string;",
    "ActivityFeed": "/**\n   * Feed rows, newest first. Item shape:\n   *   {\n   *     id: string;                 // stable key\n   *     title: React.ReactNode;     // bold lead line, e.g. \"Stamp issued\"\n   *     description?: React.ReactNode;\n   *     timestamp?: string;         // ISO string; right-aligned, formatted en-GB\n   *     metadata?: React.ReactNode; // small muted line under the description\n   *     tone?: \"accent\" | \"ink\" | \"leaf\" | \"sun\" | \"plain\"; // leading dot colour\n   *   }\n   */\n  items: ActivityFeedItem[];\n  /** Rendered in place of the list when `items` is empty (e.g. an <EmptyState>). */\n  emptyState?: React.ReactNode;\n  className?: string;",
    "DataTable": "/** Accessible table caption (visually hidden via <caption class=\"sr-only\">). */\n  caption: string;\n  /**\n   * Column definitions. Column shape (DataTableColumn<T>):\n   *   {\n   *     key: string;                      // React key + cell key\n   *     header: React.ReactNode;          // rendered inside an uppercase Eyebrow\n   *     cell: (row: T) => React.ReactNode; // per-row cell renderer\n   *     className?: string;               // applied to <th> and <td>\n   *   }\n   */\n  columns: DataTableColumn<T>[];\n  /** Row data; `T` is your row type. */\n  rows: T[];\n  /** Stable key per row. */\n  getRowKey: (row: T, index: number) => string;\n  /** Rendered instead of the table body when `rows` is empty. */\n  emptyState?: React.ReactNode;\n  className?: string;\n  rowClassName?: (row: T, index: number) => string | undefined;\n  onRowClick?: (row: T, index: number) => void;\n  /** Per-row a11y/interaction attrs on the <tr> (and mobile card <li>) — tabIndex, role, aria-*, onKeyDown for keyboard-operable rows. */\n  getRowProps?: (row: T, index: number) => Record<string, unknown> | undefined;\n  /**\n   * Opt-in mobile renderer. When set, phone widths (below `cardBreakpoint`)\n   * show this card per row instead of a horizontally scrolling table.\n   */\n  mobileCard?: (row: T, index: number) => React.ReactNode;\n  /** Class on the mobile-card list (only when `mobileCard` is set). */\n  mobileClassName?: string;\n  /** Progressive reveal for the mobile card stack: phones start with this many cards + a \"Show more\" control. Ignored when onRowClick/getRowProps are set. */\n  mobilePageSize?: number;\n  /**\n   * Breakpoint at which the card stack switches to the semantic table\n   * (only when `mobileCard` is set). Default \"sm\".\n   */\n  cardBreakpoint?: \"sm\" | \"lg\" | \"xl\";",
    "FunnelChart": "/**\n   * Funnel steps, top → bottom. Item shape (FunnelChartItem):\n   *   { label: string; value: number }\n   * Bar width is `value / max(values)`, so order steps descending for a\n   * classic narrowing funnel.\n   */\n  items: FunnelChartItem[];\n  className?: string;",
    "FormField": "/** Connects label/description/error ids to the control; use the same id on the child input's `id`. */\n  id: string;\n  label: React.ReactNode;\n  /** Helper text under the control. */\n  description?: React.ReactNode;\n  /** Validation message; when set, the field is marked `data-invalid`. */\n  error?: React.ReactNode;\n  /** The form control to wrap (e.g. an <Input>). */\n  children: React.ReactNode;",
    "FormMessage": "/** Optional id, so a control can reference it via `aria-describedby`. */\n  id?: string;\n  children: React.ReactNode;",
    "ProgressTrack": "/** Stamps earned so far. */\n  current: number;\n  /** Total stamps in the card. */\n  total: number;\n  /** Label shown above the bar; the `current / total` leaf MonoTag sits opposite. */\n  label?: string;\n  className?: string;",
    "QrFrame": "/** The QR graphic to frame (an <svg>/<img>/<canvas> matrix). */\n  children: React.ReactNode;\n  /** Accessible name on the framing <figure>. */\n  label?: string;\n  className?: string;",
    "RewardCelebration": "/** Headline for the card-complete beat (e.g. \"Card complete!\"). */\n  title: React.ReactNode;\n  /** Supporting line under the title. */\n  message: React.ReactNode;",
    "RewardChip": "/** Slot status — drives the seal glyph + sun→leaf colour. */\n  slotState?: \"locked\" | \"ready\" | \"revealed\";\n  /** Reward name woven into the seal's aria-label. */\n  label?: string;\n  /** Fire the print-pop keyframe when the slot reveals on a full card. */\n  slammed?: boolean;\n  /** Idle-wiggle the sealed mystery (join-journey preview only). */\n  placeholder?: boolean;\n  /** Shrink the chip (~36px) for dense, non-interactive preview rows. */\n  compact?: boolean;\n  className?: string;",
    "RewardSeal": "/** Reward lifecycle state — picks the glyph + sun/leaf fill. */\n  state: \"sealed\" | \"waiting\" | \"ready\" | \"redeemed\";\n  /** Disc size: sm (row chip) / md (ticket stub) / lg (celebration beat). */\n  size?: \"sm\" | \"md\" | \"lg\";\n  /** Override the state's default aria-label (e.g. to name the reward). */\n  label?: string;\n  /** Idle wiggle — only ever honoured on the `sealed` mystery. */\n  wiggle?: boolean;\n  /** Resting breathe — only ever honoured while `waiting`/`ready`. */\n  breathe?: boolean;\n  /** Fire the print-pop keyframe on reveal / redeem. */\n  slammed?: boolean;\n  className?: string;",
    "RewardTeaser": "/** @deprecated Use RewardTicket. Locked → sealed ticket, false → ready ticket. */\n  locked: boolean;\n  /** Reward (or sealed mystery) title. */\n  title: React.ReactNode;\n  description?: React.ReactNode;\n  /** Accepted for back-compat; the ticket stub always carries its own seal. */\n  hideSeal?: boolean;\n  className?: string;",
    "RewardTicket": "/** Reward lifecycle state — drives the kicker, stub word, seal + leaf slam. */\n  state: \"sealed\" | \"waiting\" | \"ready\" | \"redeemed\";\n  /** Reward name once revealed, or the sealed mystery title. */\n  name: React.ReactNode;\n  description?: React.ReactNode;\n  /** UK business day the reward opens — printed as a sun chip while `waiting`. */\n  readyDate?: string | null;\n  /** Fire the print-pop on the stub seal — e.g. hero reveal loop. */\n  sealSlammed?: boolean;\n  /** Override the state kicker — e.g. \"Clear reward\" on merchant previews. */\n  eyebrow?: string;\n  /** Heading element for the reward name (a11y). @default \"h3\" */\n  headingLevel?: \"h2\" | \"h3\";\n  className?: string;",
    "StampDot": "/** Filled (vermillion stamp) vs empty (dashed outline) slot. */\n  earned: boolean;\n  /** Accessible label, e.g. \"Stamp 3 earned\". */\n  label: string;\n  /** Stamp date printed under the initials when earned (e.g. \"14 Jun\"). */\n  date?: string;\n  /** 1-based slot position (used for the empty-slot number). */\n  slotNumber?: number;\n  /** Print `slotNumber` inside empty slots. */\n  showEmptySlotNumber?: boolean;\n  /** Fire the WetInkSlam print-pop (only on a freshly earned stamp). */\n  slammed?: boolean;\n  /** Shrink the slot (~36px) and tighten the date for dense rows. */\n  compact?: boolean;\n  /** Venue name → derives the stamped initials when `venueInitials` is absent. */\n  venueName?: string;\n  /** Explicit two-letter initials stamped on earned slots (wins over `venueName`). */\n  venueInitials?: string;\n  /** Fires when the slammed print-pop completes. */\n  onSlamComplete?: () => void;\n  className?: string;",
    "StampJourneyPreview": "/** Number of stamp slots before the reward chip (capped at 6 columns). */\n  total: number;\n  /** Venue name → stamp initials on the looping earned slots. */\n  venueName?: string;\n  className?: string;",
    "StatusBanner": "/** Banner heading (bold). */\n  title: React.ReactNode;\n  /** Optional body copy under the title. */\n  children?: React.ReactNode;\n  /** Semantic tone — sets the spot-ink wash + paired status glyph. */\n  tone?: \"success\" | \"warning\" | \"error\" | \"neutral\";\n  className?: string;",
    "Button": "/** Visual style. `stamp`/`reward` are the Wet Ink loyalty accents. */\n  variant?: \"default\" | \"stamp\" | \"reward\" | \"secondary\" | \"outline\" | \"ghost\" | \"destructive\" | \"link\";\n  /** Control height. `icon*` sizes are square. */\n  size?: \"xs\" | \"sm\" | \"default\" | \"lg\" | \"xl\" | \"icon\" | \"icon-xs\" | \"icon-sm\" | \"icon-lg\";\n  /** Render as the child element (Radix Slot) instead of a <button>. */\n  asChild?: boolean;\n  disabled?: boolean;\n  className?: string;\n  children?: React.ReactNode;",
    "PageTitle": "/** Small uppercase kicker above the title. */\n  eyebrow?: React.ReactNode;\n  /** Page heading (renders as <h1>, display weight). */\n  title: React.ReactNode;\n  description?: React.ReactNode;\n  /** Right-aligned action slot, e.g. <Button>s. */\n  actions?: React.ReactNode;\n  className?: string;\n  titleClassName?: string;\n  descriptionClassName?: string;",
    "StampGrid": "/** Stamps earned so far. */\n  current: number;\n  /** Total stamps in the card. */\n  total: number;\n  /** ISO date per earned slot, shown on the stamp. */\n  dates?: string[];\n  /** Index to fire the print-pop animation on (a freshly earned stamp). */\n  slamIndex?: number;\n  showEmptySlotNumbers?: boolean;\n  /** Append a reward chip after the stamps. */\n  rewardSlot?: \"locked\" | \"ready\" | \"revealed\";\n  /** Render total-1 earned, for join/onboarding previews. */\n  previewJourney?: boolean;\n  /** Shrink slots (~36px) for dense rows. */\n  compact?: boolean;\n  /** Single row (default) or wrapped rows for narrow surfaces. @default \"row\" */\n  layout?: \"row\" | \"wrap\";\n  /** adaptive (default) wraps discs via auto-fit when space is tight; horizontal keeps every slot on one row at the minimum track size (dense tables). @default \"adaptive\" */\n  flow?: \"adaptive\" | \"horizontal\";\n  /** Max slots per row when layout=\"wrap\" (stamps + reward chip). @default 3 */\n  wrapColumns?: number;\n  /** Reserve an always-readable \"current / total\" mono count — progress stays legible where discs render tiny. */\n  showCount?: boolean;\n  /** Count above the grid (default) or inline before the row. @default \"above\" */\n  countPlacement?: \"above\" | \"inline\";\n  venueName?: string;\n  venueInitials?: string;\n  /** Fires when the slammed stamp's print-pop completes. */\n  onSlamComplete?: () => void;\n  className?: string;",
    "Skeleton": "  // Shimmer placeholder. No intrinsic size — set dimensions via className (e.g. \"h-4 w-32\", \"size-9 rounded-full\").\n  className?: string;\n  // + native <div> props",
    "Spinner": "  // Loading spinner (animated). Size via className (e.g. \"size-5\"); colour inherits currentColor.\n  className?: string;\n  // + native <svg> props",
    "Textarea": "  placeholder?: string;\n  disabled?: boolean;\n  /** aria-invalid flips to the destructive border/ring. */\n  \"aria-invalid\"?: boolean;\n  className?: string;\n  // + native <textarea> props (value, rows, onChange, …)",
    "Table": "  // Wraps a native <table> in an overflow container; compose with TableHeader / TableBody / TableRow / TableHead / TableCell / TableCaption.\n  className?: string;\n  // + native <table> props",
    "CategoryBadge": "/** Merchant activity category — picks the spot-ink tone + leading glyph (customer=cobalt, stamp=vermillion, reward=leaf, qr=sun, account=plain). */\n  category: \"customer\" | \"stamp\" | \"reward\" | \"qr\" | \"account\";\n  /** Badge text, e.g. \"New member\". */\n  label: string;\n  className?: string;",
    "FilterPills": "/**\n   * Pills, in display order. Item shape (FilterPillItem):\n   *   { id: string; label: string; count?: number }  // count renders as a trailing chip\n   */\n  items: FilterPillItem[];\n  /** id of the selected pill (vermillion fill). */\n  value: string;\n  onValueChange: (id: string) => void;\n  /** Accessible name for the pill group. */\n  \"aria-label\"?: string;\n  className?: string;",
    "IconRoundel": "/** Glyph mode — a Hugeicons glyph rendered through the brand Icon wrapper. */\n  icon?: IconGlyph;\n  iconSize?: number;\n  iconStrokeWidth?: number;\n  /** Disc size: sm 32px (numbered step discs) / md 40px / lg 44px. @default \"lg\" */\n  size?: \"sm\" | \"md\" | \"lg\";\n  /** @default \"secondary\" */\n  tone?: \"secondary\" | \"accent\" | \"card\" | \"primary\";\n  /** Announce the roundel; omitted = decorative (aria-hidden). */\n  label?: string;\n  /** Content mode — a step number or short mark instead of a glyph. */\n  children?: React.ReactNode;\n  className?: string;",
    "KpiTile": "/** Uppercase mono stat label. */\n  label: React.ReactNode;\n  /** The figure (rendered tabular, extrabold). */\n  value: React.ReactNode;\n  /** Delta-vs-last-week caption; colour follows direction (up=leaf, down=destructive). Pass null to omit. */\n  trend?: { label: string; direction: \"up\" | \"down\" | \"flat\" } | null;\n  /** Optional leading glyph from `@hugeicons/core-free-icons`. */\n  icon?: IconGlyph;\n  /** 14-day series powering the inline sparkline (hidden below ~360px tiles). */\n  series?: number[];\n  /** Sparkline colour; defaults to the trend colour. */\n  seriesColor?: string;\n  className?: string;",
    "MemberMark": "/** Member initials (first 2 chars, uppercased). */\n  initials: string;\n  /** Ledger state — drives border/fill (ready=leaf, waiting=sun, new=vermillion wash, quiet/redeemed=muted). @default \"collecting\" */\n  tone?: \"ready\" | \"waiting\" | \"new\" | \"quiet\" | \"redeemed\" | \"collecting\";\n  /** Accessible name when no adjacent text names the member. */\n  label?: string;\n  /** Diameter in px (font scales from it). @default 36 */\n  size?: number;\n  className?: string;",
    "ShowMoreList": "/**\n   * Full item list (all items stay rendered for crawl/print). Item shape (ShowMoreListItem):\n   *   { key: string; content: React.ReactNode }\n   */\n  items: ShowMoreListItem[];\n  /** How many items render before the \"Show N more\" button. */\n  initialCount: number;\n  /** How many more each press reveals. @default initialCount */\n  step?: number;\n  /** Accessible name for the list. */\n  label: string;\n  className?: string;\n  /** Class on the inner <ul>. */\n  listClassName?: string;",
    "Sparkline": "/** Ordered series values, oldest first (needs ≥2 finite points; fewer renders a dashed flat line). */\n  data: number[];\n  /** Any CSS colour. @default \"var(--primary)\" */\n  color?: string;\n  /** Rendered height in px; width fills the container. @default 32 */\n  height?: number;\n  /** Draw the faint area fill under the line. @default true */\n  fill?: boolean;\n  /** When set the sparkline is announced; otherwise decorative. */\n  \"aria-label\"?: string;\n  className?: string;",
    "StatStrip": "/**\n   * Stat cells (2×2 on phones, 1×4 at sm+ when 4+ items; hairline-ruled). Item shape (StatStripItem):\n   *   { label: React.ReactNode; value: React.ReactNode; tone?: \"primary\" | \"cobalt\" | \"leaf\" | \"sun\" | \"ink\"; icon?: React.ReactNode }\n   */\n  items: StatStripItem[];\n  className?: string;",
    "TrendChart": "/**\n   * Lines to plot on a shared 0-based y-scale. Series shape (TrendSeries):\n   *   { label: string; color: string; data: number[]; fill?: boolean }  // fill = faint area under the primary line\n   */\n  series: TrendSeries[];\n  /** Mono caption at the oldest (left) edge of the x-axis. */\n  startLabel?: string;\n  /** Mono caption at the newest (right) edge. */\n  endLabel?: string;\n  /** Accessible description of the chart. */\n  \"aria-label\"?: string;\n  className?: string;",
    "SelectField": "  // Native <select> props (value, onChange, disabled, children = <option>s, …).\n  // Styled to match Input (data-slot=\"input\") with a chevron affordance; wrap in FormField for label/error wiring.",
    "SubmitButton": "/** Copy shown while the surrounding form's server action is in flight, e.g. \"Saving…\". Falls back to children. */\n  pendingLabel?: React.ReactNode;\n  // + all Button props except `type` (always \"submit\"). Reads useFormStatus() — pending state only activates inside a <form action={…}>."
  },
  "componentSrcMap": {
    "Alert": "components/ui/alert.tsx",
    "Badge": "components/ui/badge.tsx",
    "Button": "components/ui/button.tsx",
    "Card": "components/ui/card.tsx",
    "Empty": "components/ui/empty.tsx",
    "Field": "components/ui/field.tsx",
    "Input": "components/ui/input.tsx",
    "Label": "components/ui/label.tsx",
    "Progress": "components/ui/progress.tsx",
    "Separator": "components/ui/separator.tsx",
    "Sheet": "components/ui/sheet.tsx",
    "Sidebar": "components/ui/sidebar.tsx",
    "Skeleton": "components/ui/skeleton.tsx",
    "Spinner": "components/ui/spinner.tsx",
    "Table": "components/ui/table.tsx",
    "Textarea": "components/ui/textarea.tsx",
    "Toaster": "components/ui/sonner.tsx",
    "Logo": "components/brand/logo.tsx",
    "MonoTag": "components/brand/mono-tag.tsx",
    "ReceiptCard": "components/brand/receipt-card.tsx",
    "VenueMark": "components/brand/venue-mark.tsx",
    "Icon": "components/brand/icon.tsx",
    "EmptyState": "components/brand/typography.tsx",
    "Eyebrow": "components/brand/typography.tsx",
    "MetricTile": "components/brand/typography.tsx",
    "PageTitle": "components/brand/typography.tsx",
    "SectionHeader": "components/brand/typography.tsx",
    "ActivityFeed": "components/data/activity-feed.tsx",
    "DataTable": "components/data/data-table.tsx",
    "FunnelChart": "components/data/funnel-chart.tsx",
    "FormField": "components/forms/form-field.tsx",
    "FormMessage": "components/forms/form-field.tsx",
    "ProgressTrack": "components/loyalty/progress-track.tsx",
    "QrFrame": "components/loyalty/qr-frame.tsx",
    "RewardCelebration": "components/loyalty/reward-celebration.tsx",
    "RewardChip": "components/loyalty/stamp-grid.tsx",
    "RewardSeal": "components/loyalty/reward-seal.tsx",
    "RewardTeaser": "components/loyalty/reward-teaser.tsx",
    "RewardTicket": "components/loyalty/reward-ticket.tsx",
    "StampDot": "components/loyalty/stamp-grid.tsx",
    "StampGrid": "components/loyalty/stamp-grid.tsx",
    "StampJourneyPreview": "components/loyalty/stamp-journey-preview.tsx",
    "StatusBanner": "components/loyalty/status-banner.tsx",
    "CategoryBadge": "components/brand/category-badge.tsx",
    "FilterPills": "components/brand/filter-pills.tsx",
    "IconRoundel": "components/brand/icon-roundel.tsx",
    "KpiTile": "components/brand/kpi-tile.tsx",
    "MemberMark": "components/brand/member-mark.tsx",
    "ShowMoreList": "components/data/show-more-list.tsx",
    "Sparkline": "components/data/sparkline.tsx",
    "StatStrip": "components/data/stat-strip.tsx",
    "TrendChart": "components/data/trend-chart.tsx",
    "SelectField": "components/forms/select-field.tsx",
    "SubmitButton": "components/forms/submit-button.tsx"
  },
  "overrides": {
    "Table": {
      "cardMode": "column"
    },
    "DataTable": {
      "cardMode": "column"
    },
    "FunnelChart": {
      "cardMode": "column"
    },
    "ActivityFeed": {
      "cardMode": "column"
    },
    "Sheet": {
      "cardMode": "single",
      "viewport": "460x600"
    },
    "Sidebar": {
      "cardMode": "single",
      "viewport": "360x540"
    },
    "ShowMoreList": {
      "cardMode": "column"
    },
    "StatStrip": {
      "cardMode": "column"
    },
    "TrendChart": {
      "cardMode": "column"
    }
  },
  "readmeHeader": ".design-sync/conventions.md"
}
