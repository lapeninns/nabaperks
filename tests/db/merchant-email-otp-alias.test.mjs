import { test } from "node:test"

// Harness placeholder required before the draft Micro-Spec can be activated.
// The first active-spec implementation step replaces this with RED lifecycle
// assertions against the live local Supabase database.
test.todo("MS-auth-otp-alias-finalization database lifecycle")
