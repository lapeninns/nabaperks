import { describeOfferCampaignJourney } from "./offer-campaign-flow"

/**
 * The same journey on the desktop projects. The merchant desk and the creator
 * are worked from a laptop behind the bar, and both switch layout above the
 * large breakpoint — the review step puts the customer preview in its own
 * sticky column — so the desktop run is not a duplicate of the mobile one.
 */
describeOfferCampaignJourney()
